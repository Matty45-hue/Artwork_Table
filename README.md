# Artwork_Table
A React + TypeScript application that displays artwork data from the Art Institute of Chicago API using PrimeReact DataTable. Features include server-side pagination, persistent row selection, and a custom row selection panel. Built with Vite for fast development and optimized for production deployment
